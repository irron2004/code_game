@AGENTS.md

## Claude Code 전용

`algorithm-game/`은 브라우저에서 눈으로 확인해야 하는 캔버스 UI다. 화면 동작을 확인해야 하는
요청("실행해봐", "스크린샷", "동작 확인")에는 `run` 스킬로 `npm start` 후 `http://localhost:8080`을
띄워 확인한다. 로직만 바꾼 경우엔 `npm test`로 충분하다.
