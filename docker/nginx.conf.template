server {
    listen ${PORT};
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    charset utf-8;

    location / {
        try_files $uri $uri/ =404;
    }

    add_header Cache-Control "no-store";
}
