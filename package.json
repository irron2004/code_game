{
  "name": "algorithm-learning-game",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "vitest --run",
    "test:watch": "vitest",
    "check": "node --check server.js && vitest --run"
  },
  "dependencies": {
    "express": "^4.22.2",
    "express-rate-limit": "^6.11.2",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
